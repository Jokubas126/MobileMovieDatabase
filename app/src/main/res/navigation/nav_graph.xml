<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/titleScreenFragment">

    <fragment
        android:id="@+id/titleScreenFragment"
        android:name="com.example.moviesearcher.view.TitleScreenFragment"
        android:label="Movie Searcher"
        tools:layout="@layout/fragment_title_screen">
        <action
            android:id="@+id/action_movies_list"
            app:destination="@id/movieList" />
    </fragment>

    <navigation
        android:id="@+id/movieList"
        android:label="movieList"
        app:startDestination="@id/moviesList">
        <fragment
            android:id="@+id/moviesList"
            android:name="com.example.moviesearcher.view.grid.MoviesGridFragment"
            android:label="Movies"
            tools:layout="@layout/fragment_movies_grid"/>
        <action
            android:id="@+id/action_movie_details"
            app:destination="@id/movieDetails" />
    </navigation>

    <navigation
        android:id="@+id/movieDetails"
        android:label="movieDetails"
        app:startDestination="@id/movieOverview">

        <argument
            android:name="movieId"
            app:argType="integer" />

        <fragment
            android:id="@+id/movieCast"
            android:name="com.example.moviesearcher.view.details.MovieCastFragment"
            android:label="Cast and Crew"
            tools:layout="@layout/fragment_movie_cast">
            <argument
                android:name="movieId"
                app:argType="integer" />
        </fragment>
        <fragment
            android:id="@+id/movieTrailers"
            android:name="com.example.moviesearcher.view.details.MovieTrailersFragment"
            android:label="Trailers"
            tools:layout="@layout/fragment_movie_trailers">
            <argument
                android:name="movieId"
                app:argType="integer" />
        </fragment>
        <fragment
            android:id="@+id/movieOverview"
            android:name="com.example.moviesearcher.view.details.MovieOverviewFragment"
            android:label="Overview"
            tools:layout="@layout/fragment_movie_overview">
            <argument
                android:name="movieId"
                app:argType="integer" />
        </fragment>
        <action
            android:id="@+id/action_movie_cast"
            app:destination="@id/movieCast" />
        <action
            android:id="@+id/action_movie_trailers"
            app:destination="@id/movieTrailers" />
        <action
            android:id="@+id/actionMovieOverview"
            app:destination="@id/movieOverview" />

    </navigation>

</navigation>