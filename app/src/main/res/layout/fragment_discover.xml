<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.example.mmdb.ui.discover.DiscoverViewModel" />

        <import type="java.text.DecimalFormatSymbols" />

        <import type="android.view.View" />
    </data>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true">

        <include layout="@layout/view_main_background" />

        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/app_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:theme="@style/AppTheme.AppBarOverlay"
            app:expanded="true"
            app:layout_behavior=".ui.discover.ScrollingToolbarBehavior">

            <com.google.android.material.appbar.CollapsingToolbarLayout
                android:id="@+id/collapsing_toolbar_layout"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                app:layout_scrollFlags="scroll|exitUntilCollapsed|snap|enterAlways">

                <LinearLayout
                    android:id="@+id/large_view"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="?attr/actionBarSize"
                    android:background="@drawable/bg_release_date_layout"
                    android:orientation="vertical"
                    android:padding="15dp"
                    app:layout_collapseMode="parallax"
                    app:layout_collapseParallaxMultiplier="1.0">

                    <TextView
                        android:id="@+id/release_year_title"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_gravity="center_horizontal"
                        android:text="@string/release_year_title"
                        android:textAppearance="@style/ReleaseYearTextAppearance" />

                    <FrameLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content">

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:gravity="start"
                            android:text="@{viewModel.startYear == 1950 ? DecimalFormatSymbols.getInstance().infinity : String.valueOf(viewModel.startYear)}"
                            android:textAppearance="@style/ReleaseYearItemsTextAppearance"
                            tools:text="1951" />

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_gravity="end"
                            android:text="@{String.valueOf(viewModel.endYear)}"
                            android:textAppearance="@style/ReleaseYearItemsTextAppearance"
                            tools:text="2021" />

                    </FrameLayout>

                    <com.google.android.material.slider.RangeSlider
                        android:id="@+id/rangeSlider"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:stepSize="1.0"
                        android:valueFrom="@{Float.parseFloat(String.valueOf(viewModel.Companion.INITIAL_START_YEAR_VALUE))}"
                        android:valueTo="@{Float.parseFloat(String.valueOf(viewModel.Companion.INITIAL_END_YEAR_VALUE))}"
                        app:labelBehavior="gone"
                        app:thumbColor="@color/secondary_500"
                        app:thumbRadius="10dp"
                        app:tickColorActive="@color/secondary_300"
                        app:tickColorInactive="@color/secondary_100"
                        app:trackColorActive="@color/secondary_500"
                        app:trackColorInactive="@color/secondary_300"
                        app:values="@array/initial_range_slider_values" />
                </LinearLayout>

                <androidx.appcompat.widget.Toolbar
                    android:id="@+id/collapsing_toolbar"
                    android:layout_width="match_parent"
                    android:layout_height="?attr/actionBarSize"
                    android:background="@color/white_80"
                    app:contentInsetStart="0dp"
                    app:layout_collapseMode="pin"
                    app:popupTheme="@style/AppTheme.PopupOverlay">

                    <androidx.constraintlayout.widget.ConstraintLayout
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:background="@color/white"
                        android:padding="@dimen/spacing_xxs"
                        app:layout_collapseMode="pin"
                        app:popupTheme="@style/AppTheme.PopupOverlay">

                        <LinearLayout
                            android:id="@+id/expand_collapse_btn"
                            android:layout_width="wrap_content"
                            android:layout_height="match_parent"
                            android:background="?android:selectableItemBackground"
                            android:orientation="horizontal"
                            app:layout_constraintBottom_toBottomOf="parent"
                            app:layout_constraintStart_toStartOf="parent"
                            app:layout_constraintTop_toTopOf="parent">

                            <ImageView
                                android:layout_width="wrap_content"
                                android:layout_height="match_parent"
                                android:adjustViewBounds="true"
                                android:background="?android:selectableItemBackground"
                                android:onClick="@{() -> viewModel.toolbarViewModel.onBackClicked.invoke()}"
                                android:paddingStart="@dimen/spacing_s"
                                android:paddingTop="@dimen/spacing_m"
                                android:paddingEnd="@dimen/spacing_s"
                                android:paddingBottom="@dimen/spacing_m"
                                android:src="@drawable/ic_close_x"
                                app:layout_constraintStart_toStartOf="parent" />

                            <TextView
                                android:id="@+id/title_text_view"
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:layout_gravity="center_vertical"
                                android:ellipsize="end"
                                android:maxLines="1"
                                android:text="@string/discover_fragment"
                                android:textAppearance="@style/ToolbarStyle"
                                android:textColor="@color/primary" />

                            <ImageView
                                android:id="@+id/arrow_image_view"
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:layout_gravity="center_vertical"
                                android:layout_marginStart="4dp"
                                android:layout_marginEnd="4dp"
                                app:srcCompat="@drawable/ic_arrow_drop_up"
                                tools:ignore="ContentDescription" />
                        </LinearLayout>

                        <ImageView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:adjustViewBounds="true"
                            android:background="?android:selectableItemBackground"
                            android:onClick="@{() -> viewModel.toolbarViewModel.toolbarConfig.onConfirmClicked.invoke()}"
                            android:padding="@dimen/spacing_s"
                            android:src="@drawable/ic_check"
                            android:visibility="@{viewModel.toolbarViewModel.toolbarConfig.confirmButtonEnabled ? View.VISIBLE : View.GONE}"
                            app:layout_constraintBottom_toBottomOf="parent"
                            app:layout_constraintEnd_toEndOf="parent"
                            app:layout_constraintTop_toTopOf="parent" />

                    </androidx.constraintlayout.widget.ConstraintLayout>
                </androidx.appcompat.widget.Toolbar>
            </com.google.android.material.appbar.CollapsingToolbarLayout>
        </com.google.android.material.appbar.AppBarLayout>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/categories_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:nestedScrollingEnabled="false"
            android:orientation="vertical"
            app:adapter="@{viewModel.categoriesAdapter}"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>
</layout>